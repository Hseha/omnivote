// vote_now_screen.dart
// PURPOSE: Entry flow: guided ballot across all positions in order (school tier then provincial tier), one position per step, with progress indicator (e.g. 1/16).
// STATUS: stub — implement per docs/04_SCREENS_SPEC.md
