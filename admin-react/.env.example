# Vite environment variables are prefixed with VITE_ and exposed to the client.
# Leave empty when the admin panel is served from the same origin as the API.
# Set to the absolute API origin (e.g. https://debian.tail7e9e1e.ts.net) when served
# cross-origin (separate web server / CDN).
VITE_API_BASE_URL=